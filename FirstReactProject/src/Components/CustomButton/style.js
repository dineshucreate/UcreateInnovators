import { StyleSheet } from 'react-native';

const myStyle = StyleSheet.create({
    container: {
        fontSize: 20,
        fontWeight: 'bold',
        marginTop: 40,
      },
});
export default myStyle;
